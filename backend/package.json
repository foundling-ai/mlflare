{
  "name": "mlflare-backend",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run --outdir=../embedded/worker",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "hono": "^4.11.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250109.0",
    "typescript": "^5.7.0",
    "wrangler": "^3.99.0"
  }
}
